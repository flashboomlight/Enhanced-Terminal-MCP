---
doc_type: feature-checklist
feature: 2026-08-29-tool-wrapper-and-surface-contract
roadmap: production-hardening
roadmap_item: tool-wrapper-and-surface-contract
status: done
created: "2026-08-29"

steps:
  - action: "新增 src/tool-registry.ts（registerManagedTool 记账、getRegisteredToolCount 启用数、getEnabledToolNames/getAllRegisteredToolNames），wrap.ts 删除自增计数，index/utility 改 import"
    exit_signal: "计数唯一真源为 SDK RegisteredTool.enabled；tools 内 server.registerTool 调用点零残留"
    status: done
  - action: "wrapHandler 增加 try/catch 边界：未预期异常经 redactError 映射 INTERNAL_ERROR、aborted/AbortError 映射 CANCELLED、telemetry 记录、错误不入缓存"
    exit_signal: "handler throw 不再产生 rejected promise；错误 message 不泄露原始命令/凭据"
    status: done
  - action: "响应字节兜底：MCP_RESPONSE_MAX_BYTES（默认 2097152，strict 解析非法回落+warn），wrapHandler 在 toCallToolResult 前度量 content+structuredContent"
    exit_signal: "超限返回 RESOURCE_LIMIT 错误 envelope 且 detail 仅有限元；不提供无限制取值"
    status: done
  - action: "三个 action 工具 handler 层缺参显式拒绝：session_state(set_cwd⇒cwd、set_env⇒key/value)、environment_vars(get⇒name)、network_info(ping/dns⇒target，删除隐式默认)"
    exit_signal: "缺参返回 VALIDATION_ERROR（param 指向缺失字段）；静默 no-op 分支零残留；schema .describe() 标注条件必填"
    status: done
  - action: "capability 矩阵接线：profile.ts 新增 capabilityGate，process_list/get_system_info/network_info/download_file/environment_vars 五面接入"
    exit_signal: "local-trusted-shell 全放行零行为变化；sandboxed 未声明返回 CAPABILITY_DENIED"
    status: done
  - action: "surface 一致性：health resource 增加 tools.enabled/disabled，扩展 tests/tool-visibility.test.ts 五断言（27/26、prompt 子串、health 字段、pool_stats.active=false、session_state 缺参 MCP 全链路）"
    exit_signal: "两种配置下 health/prompt/tools/list 同源一致"
    status: done
  - action: "单测补全（wrap 边界/响应口径、tool-registry、capabilityGate、tools 层缺参）并更新 README/CHANGELOG"
    exit_signal: "验收场景逐条有断言；README 环境变量与工具描述同步"
    status: done
  - action: "定向回归、全量门禁（build/tsc/lint/test/latency/tools coverage）、多轮反向审计与 CodeStable 回写"
    exit_signal: "全门禁 EXIT=0；roadmap/items/architecture/audit explore 回写；YAML 校验通过"
    status: done

checks:
  - item: "handler 未预期 throw 返回 INTERNAL_ERROR ToolResult（直调与 MCP 双路径），message 经 redactor 净化"
    source: "设计 3.2 / 验收句 1"
    status: done
  - item: "signal.aborted 或 AbortError 逃逸映射 CANCELLED（retryable）"
    source: "设计 3.2"
    status: done
  - item: "默认工具数 27、ENHANCED_TERMINAL_DISABLE_FILE_INFO=1 时 26（tools/list）"
    source: "设计 3.1/3.6 / 验收句 2"
    status: done
  - item: "两种配置下 usage-guide 文本、health.tools.enabled/disabled、tools/list 三面数字一致"
    source: "设计 3.6 / 验收句 3"
    status: done
  - item: "session_state set_cwd 缺 cwd / set_env 缺 key 或 value 返回 VALIDATION_ERROR，不再静默快照"
    source: "设计 3.4 / 验收句 4"
    status: done
  - item: "environment_vars get 缺 name 返回 VALIDATION_ERROR，不再静默降级为 list"
    source: "设计 3.4 / 验收句 4"
    status: done
  - item: "network_info ping/dns 缺 target 返回 VALIDATION_ERROR，隐式 127.0.0.1/localhost 默认删除"
    source: "设计 3.4 / 验收句 4"
    status: done
  - item: "响应超过 MCP_RESPONSE_MAX_BYTES 返回 RESOURCE_LIMIT；配置非法回落默认并 warn"
    source: "设计 3.3 / 4"
    status: done
  - item: "capabilityGate：local 放行、sandboxed 未声明拒绝、声明后放行；五披露面接线位置在 spawn 前"
    source: "设计 3.5"
    status: done
  - item: "pool_stats active=false 与文案一致（PRO-02 证据固化）"
    source: "设计 3.6"
    status: done
  - item: "wrapHandler (args, extra) 适配固化：extra.requestId/sessionId/signal 注入 context，伪造 arguments 不可覆盖"
    source: "设计 3.2 / 契约 §5.0"
    status: done

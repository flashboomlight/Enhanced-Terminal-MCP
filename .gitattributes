# 统一换行符为 LF
* text=auto eol=lf
# 二进制文件
*.exe binary
*.dll binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.db binary
# es_tool/*.exe 直接以普通二进制入库（136KB，曾用 LFS，为免克隆端依赖 git-lfs 已拆除）

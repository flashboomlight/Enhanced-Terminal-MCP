diff --git a/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/mcp.js b/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/mcp.js
index 9e1c874..4922ec8 100644
--- a/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/mcp.js
+++ b/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/mcp.js
@@ -77,12 +77,17 @@ class McpServer {
                     description: tool.description,
                     inputSchema: (() => {
                         const obj = (0, zod_compat_js_1.normalizeObjectSchema)(tool.inputSchema);
-                        return obj
+                        const schema = obj
                             ? (0, zod_json_schema_compat_js_1.toJsonSchemaCompat)(obj, {
                                 strictUnions: true,
                                 pipeStrategy: 'input'
                             })
                             : EMPTY_OBJECT_JSON_SCHEMA;
+                        // PATCH: Ensure `required` is always an array for object schemas.
+                        if (schema && schema.type === 'object' && !schema.hasOwnProperty('required')) {
+                            schema.required = [];
+                        }
+                        return schema;
                     })(),
                     annotations: tool.annotations,
                     execution: tool.execution,
@@ -811,7 +816,9 @@ class ResourceTemplate {
 exports.ResourceTemplate = ResourceTemplate;
 const EMPTY_OBJECT_JSON_SCHEMA = {
     type: 'object',
-    properties: {}
+    properties: {},
+    // PATCH: Strict validators require `required` to be an explicit array
+    required: []
 };
 /**
  * Checks if a value looks like a Zod schema by checking for parse/safeParse methods.
diff --git a/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js b/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js
index 2314d88..6ef1b07 100644
--- a/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js
+++ b/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js
@@ -74,12 +74,18 @@ export class McpServer {
                     description: tool.description,
                     inputSchema: (() => {
                         const obj = normalizeObjectSchema(tool.inputSchema);
-                        return obj
+                        const schema = obj
                             ? toJsonSchemaCompat(obj, {
                                 strictUnions: true,
                                 pipeStrategy: 'input'
                             })
                             : EMPTY_OBJECT_JSON_SCHEMA;
+                        // PATCH: Ensure `required` is always an array for object schemas.
+                        // Strict validators (OpenAI, DeepSeek, SGLang) require it.
+                        if (schema && schema.type === 'object' && !schema.hasOwnProperty('required')) {
+                            schema.required = [];
+                        }
+                        return schema;
                     })(),
                     annotations: tool.annotations,
                     execution: tool.execution,
@@ -806,7 +812,9 @@ export class ResourceTemplate {
 }
 const EMPTY_OBJECT_JSON_SCHEMA = {
     type: 'object',
-    properties: {}
+    properties: {},
+    // PATCH: Strict validators require `required` to be an explicit array
+    required: []
 };
 /**
  * Checks if a value looks like a Zod schema by checking for parse/safeParse methods.
